# TypeScript React movie-app-front Readme

##Running the Client

1. Open a new terminal and navigate to the client folder of the project.

2. Install the client-side dependencies by running: npm install

3. Start the client-side development server by running: npm start

The client application should now be accessible at http://localhost:3000
